## Lombok 配置
## 允许把 Spring 的 @Lazy 注解从字段复制到 @RequiredArgsConstructor 生成的构造参数上，
## 用于打破 Service 间循环依赖（规范：出现循环依赖用 @Lazy，不改回字段注入）。
config.stopBubbling = true
lombok.copyableAnnotations += org.springframework.context.annotation.Lazy
