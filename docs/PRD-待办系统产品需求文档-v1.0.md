# 待办系统产品需求文档（PRD）

> **文档版本**：v1.0（初版）
> **文档负责人**：产品经理
> **输出对象**：前端开发 AI、后端开发 AI
> **更新日期**：2026-06-28
> **阅读说明**：本文档为待办系统的完整产品需求规格。所有功能模块均包含「功能说明」「业务规则」「交互逻辑」「数据要点」四部分，前后端可直接据此实现。涉及页面设计的章节会标注「前端实现要点」，涉及数据与接口的会标注「后端实现要点」。

---

## 0. 文档导读

本文档面向「实现人员」（含 AI 编码助手），目的是让待办系统从 0 到 1 落地。为降低理解成本，统一约定如下术语：

| 术语 | 含义 |
|------|------|
| 待办（Todo） | 系统核心对象，一条需要用户执行的事项 |
| 任务（Task） | 本文中等同于待办，不单独区分 |
| 大分类（Category） | 待办的第一层分类，如「公司事务」「生活事务」 |
| 子分类（Subcategory） | 待办的第二层分类，如「A 项目」「学习成长」 |
| 计划日期（Plan Date） | 待办计划完成的日期，默认为当天 |
| 完成日期（Done At） | 待办被标记完成的时间 |

### 文档结构

1. 项目背景与目标
2. 产品定位与核心价值
3. 用户角色与权限
4. 全局规则（命名、状态、时间等通用约定）
5. 功能模块总览（功能架构图）
6. 功能详细需求（逐模块拆解）
7. 全局业务流程
8. 非功能需求
9. 数据模型概览（供后端参考）
10. 页面清单与路由（供前端参考）
11. 接口清单概览（供后端参考）
12. 版本规划（分期建议）
13. 验收标准与审核清单（产品负责人自用）

---

## 1. 项目背景与目标

### 1.1 背景

用户长期处于工作繁忙状态，日常需要处理大量来自公司、生活、学习等多方面的待办事项。但公司并未提供一套趁手的待办管理工具，现有笔记类工具又偏重记录、轻管理，导致：

- 事项散落在便签、聊天记录、大脑记忆中，容易遗漏；
- 无法快速区分「今天必须做」和「以后再做」；
- 缺乏完成情况的回顾，难以衡量自己的时间投入与产出。

### 1.2 目标

构建一套**个人专属、轻量高效、可分类、可回顾**的待办管理系统，帮助用户：

1. **集中收纳**：所有待办事项统一在一个入口管理，不再遗漏；
2. **聚焦当天**：默认聚焦今日待办，让每天的工作有明确焦点；
3. **分类清晰**：两层分类体系，让公司事务与生活事务井井有条；
4. **自动流转**：今日未完成事项次日自动延续，无需手动搬迁；
5. **持续提效**：通过回顾与统计，帮助用户持续优化时间分配。

### 1.3 成功指标（建议）

| 指标 | 目标 |
|------|------|
| 日活待办创建数 | 单用户每日平均创建 ≥ 3 条 |
| 今日完成率 | 周平均今日完成率 ≥ 70% |
| 次日自动延续触发率 | 体感上「不丢事项」 |
| 核心路径耗时 | 创建一条待办 ≤ 5 秒 |

---

## 2. 产品定位与核心价值

### 2.1 产品定位

一套**面向个人的私有化轻量待办系统**，强调「专注今日、清晰分类、自动流转、轻量回顾」。不追求企业级协作与多人派发，专注单用户的个人效率。

### 2.2 核心价值主张

- **Today First**：打开即看到今天该做什么；
- **Never Lost**：未完成的事项自动延到次日，绝不丢失；
- **Clear Bucket**：两层分类，公司 / 生活 / 学习各有归处；
- **Quick Capture**：极简创建，3 秒记下一条事项。

---

## 3. 用户角色与权限

### 3.1 角色定义

| 角色 | 说明 | 权限 |
|------|------|------|
| 普通用户 | 系统的唯一使用者（个人私有系统） | 注册、登录、管理自己的分类与待办、查看自己的统计 |
| 访客（未登录） | 未注册或未登录的访问者 | 仅可访问「首页（介绍页）」与「登录 / 注册页」 |

### 3.2 数据隔离原则（强制）

- **待办、分类、子分类数据严格按用户隔离**。用户 A 在任何情况下都无法查看、编辑、引用用户 B 的数据。
- 所有待办、分类相关接口在查询时必须强制带上 `当前登录用户 ID` 作为过滤条件，**禁止仅凭资源 ID 访问**。
- 统计、回顾数据也仅基于当前用户的数据生成。

---

## 4. 全局规则

> 本章规则贯穿全系统，所有模块共享，后续模块不再重复描述。

### 4.1 命名规范

- 待办标题：必填，最大长度 100 字符。
- 备注内容：选填，最大长度 2000 字符。
- 大分类名称：必填，最大长度 20 字符，同一用户下不可重名。
- 子分类名称：必填，最大长度 20 字符，同一大分类下不可重名。

### 4.2 待办状态机

待办有以下状态，状态流转必须遵循下方规则：

| 状态 | 英文标识 | 说明 |
|------|----------|------|
| 待处理 | `pending` | 已创建，尚未完成，等待执行 |
| 已完成 | `done` | 已被标记完成 |
| 已删除 | `deleted` | 软删除，进入回收站，可恢复 |

**状态流转规则：**

```
pending ──(标记完成)──> done
pending ──(删除)──────> deleted
done    ──(撤销完成)──> pending
done    ──(删除)──────> deleted
deleted ──(恢复)──────> 回到删除前的状态（pending 或 done）
deleted ──(彻底删除)──> 物理删除（不可恢复）
```

- 已完成可以撤销（回到 pending），删除均采用**软删除**，可在回收站恢复。

### 4.3 优先级

每个待办有一个优先级，用于排序与提示：

| 优先级 | 标识 | 视觉建议 |
|--------|------|----------|
| 高 | `high` | 红色标记 |
| 中 | `medium` | 橙色 / 默认 |
| 低 | `low` | 灰色 |

- 新建待办默认优先级为 `medium`。

### 4.4 日期与时区规则（关键）

- 系统所有「日期」概念统一按**用户本地时区**的「自然日」计算，而非 24 小时滚动。
- 「今天」= 用户当前本地日期 00:00:00 至 23:59:59。
- **计划日期**字段只精确到「天」（存储为日期，不带时分秒，或存储当天 00:00），用于「今日待办」「未完成自动延续」等判断。
- 所有时间戳（如完成时间 `doneAt`、创建时间 `createdAt`）使用 UTC 存储，展示时转为本地时区。

### 4.5 次日自动延续规则（核心业务，务必实现）

> 这是用户最看重的需求之一：「今天没完成，明天要能看到」。

**规则定义：**

- 当一个待办满足：`状态 = pending` **且** `计划日期 < 今天`，称为**逾期未完成待办**。
- 系统在用户每次打开「今日」视图时（或在每日凌晨的定时任务中），将这些逾期未完成的待办的**计划日期更新为「今天」**。
- 更新计划日期后，该待办自然出现在「今日待办」列表中。

**实现建议（后端两选一）：**

1. **懒加载方式（推荐 MVP 用）**：用户请求「今日待办」接口时，后端先把 `pending` 且 `计划日期 < 今天` 的待办的 `计划日期` 更新为今天，再返回今日列表。优点：实现简单、即时生效。
2. **定时任务方式**：每天凌晨 00:05（用户本地时区）跑一次定时任务，批量把逾期未完成待办的计划日期更新为当天。优点：数据一致性更好，列表查询更快。

- 建议两种结合：定时任务做兜底，懒加载做即时修正。
- **注意**：已完成（`done`）和已删除（`deleted`）的待办**不参与**自动延续。
- 自动延续应记录一个 `延续次数` 或在备注里标注，方便用户回顾，但 MVP 可不做。

### 4.6 排序规则

「今日待办」列表默认排序权重（从上到下）：

1. 优先级：high > medium > low；
2. 同优先级内：未完成（pending）在前，已完成（done）在后；
3. 同状态同优先级内：按创建时间升序（先创建的在前）；
4. 可选：置顶（pin）的待办始终在最前（见 6.6 节）。

### 4.7 软删除与回收站

- 删除待办、分类均为软删除（标记 `deletedAt`）。
- 待办删除后进入「回收站」，保留 30 天，30 天后由定时任务物理删除。
- 回收站内可「恢复」待办。恢复时：
  - 若其所属分类 / 子分类已被删除，则恢复到默认分类（或提示用户重新选择分类）。
- 删除大分类时：见 6.7 节的分类删除规则。

---

## 5. 功能模块总览

### 5.1 功能架构

```
待办系统
├── 首页（介绍页 / 落地页）              —— 模块 6.1
├── 账户模块                            —— 模块 6.2
│   ├── 注册
│   ├── 登录
│   └── 退出登录
├── 待办核心模块                        —— 模块 6.3
│   ├── 今日待办（默认首页）
│   ├── 待办创建
│   ├── 待办编辑
│   ├── 待办完成 / 撤销
│   ├── 待办删除 / 恢复
│   └── 待办详情
├── 分类管理模块                        —— 模块 6.4
│   ├── 大分类管理（增删改查）
│   └── 子分类管理（增删改查）
├── 视图与筛选模块                      —— 模块 6.5
│   ├── 按日期查看（今天 / 明天 / 某天）
│   ├── 按分类查看
│   └── 搜索
├── 效率增强模块                        —— 模块 6.6
│   ├── 置顶
│   ├── 提醒（到期提醒）
│   ├── 标签（可选扩展）
│   ├── 子任务 / 清单（可选扩展）
│   └── 重复待办（可选扩展）
├── 回顾统计模块                        —— 模块 6.7
│   ├── 今日概览
│   ├── 完成趋势
│   └── 分类占比
└── 设置模块                            —— 模块 6.8
    ├── 个人资料
    ├── 修改密码
    └── 偏好设置
```

### 5.2 功能优先级（P0/P1/P2）

| 优先级 | 含义 | 模块 |
|--------|------|------|
| P0 | MVP 必须有 | 首页、注册登录、今日待办、待办增删改查完成、两层分类、次日自动延续 |
| P1 | 强烈建议有 | 日期视图、分类视图、搜索、置顶、今日概览统计、回收站、提醒 |
| P2 | 后续迭代 | 重复待办、子任务、标签、完成趋势图表、导出 |

---

## 6. 功能详细需求

### 6.1 首页（介绍页 / 落地页）

**功能说明：**

面向**未登录访客**的系统介绍页，承担「产品介绍」与「引导注册登录」两个职责。已登录用户访问根路径时直接跳转「今日待办」，不展示本页。

**页面内容：**

1. **顶部导航栏**：左侧系统名称 / Logo，右侧「登录」「注册」按钮。
2. **主视觉区（Hero）**：
   - 一句主标题（如「专注今日，事事有归处」）；
   - 一句副标题，说明系统价值；
   - 一个主 CTA 按钮「立即开始」→ 跳转注册；
   - 一个次 CTA「已有账号？登录」→ 跳转登录。
3. **核心卖点区**：3～4 个特性卡片，每个卡片一个图标 + 标题 + 一句话描述。建议卖点：
   - 「今日聚焦」：打开即看到今天该做什么；
   - 「永不丢失」：未完成事项次日自动延续；
   - 「两层分类」：公司 / 生活 / 学习井井有条；
   - 「极简记录」：3 秒记下一条事项。
4. **使用步骤区**：3 步引导（注册登录 → 建分类 → 记待办），让用户快速理解。
5. **页脚**：版权信息、版本号。

**业务规则：**

- 本页**不需要登录**即可访问。
- 已登录用户访问 `/`（首页路由）时，**自动重定向**到 `/today`（今日待办）。

**前端实现要点：**

- 路由：`/`
- 未登录访问 `/today` 等需鉴权页面时，重定向到 `/login`，并在 query 中带 `redirect=/today`，登录成功后跳回。

---

### 6.2 账户模块

#### 6.2.1 注册

**功能说明：**

新用户通过账号密码注册。

**字段：**

| 字段 | 必填 | 校验规则 |
|------|------|----------|
| 账号（用户名） | 是 | 4～20 位，字母 / 数字 / 下划线，不可与已有用户重复 |
| 邮箱 | 是（建议） | 合法邮箱格式，不可重复；用于找回密码 |
| 密码 | 是 | 8～32 位，至少包含字母和数字 |
| 确认密码 | 是 | 必须与密码一致 |

**业务规则：**

- 用户名全局唯一，邮箱全局唯一，重复时给出明确错误提示。
- 密码必须**加密存储**（如 bcrypt），严禁明文。
- 注册成功后：自动登录并跳转「今日待办」。
- 注册时为用户自动创建一组**默认大分类**（见 6.4.1），降低首次使用门槛。

**前端实现要点：**

- 路由：`/register`
- 表单实时校验；提交时按钮 loading，防重复提交。
- 错误提示就近展示在对应字段下方。

#### 6.2.2 登录

**功能说明：**

已注册用户通过账号（或邮箱）+ 密码登录。

**字段：**

| 字段 | 必填 | 校验规则 |
|------|------|----------|
| 账号或邮箱 | 是 | — |
| 密码 | 是 | — |

**业务规则：**

- 登录成功后签发令牌（建议 JWT 或 Session）。
- 登录态有效期建议 7 天（可配置），支持「记住我」延长至 30 天。
- 若带 `redirect` 参数，登录成功后跳回该参数所指页面；否则跳转 `/today`。
- 登录失败时提示「账号或密码错误」，**不区分**是账号不存在还是密码错误（安全考虑）。
- 连续 5 次失败后，该账号 5 分钟内禁止登录（防爆破，P1）。

**前端实现要点：**

- 路由：`/login`
- 令牌存储建议：httpOnly Cookie（更安全）或 localStorage。
- 全局 axios 拦截器：401 时清除登录态并跳转登录页。

#### 6.2.3 退出登录

- 用户主动退出，清除前端登录态，跳转首页或登录页。
- 后端可选地将当前令牌加入黑名单（P1）。

---

### 6.3 待办核心模块

#### 6.3.1 今日待办（核心默认页）

**功能说明：**

已登录用户的主工作台，展示「今天需要处理」的所有待办。这是用户每天最常打开的页面。

**页面内容：**

1. **页面头部**：
   - 标题「今日待办」+ 当前日期（如 2026-06-28 周日）；
   - 今日统计条：`今日共 X 项 / 已完成 Y 项 / 完成率 Z%`，配一个进度条。
2. **快速创建栏**：一个输入框 +（可选）优先级 / 分类选择，回车或点按钮快速创建（见 6.3.2）。
3. **待办列表**：按 4.6 排序规则展示今日待办。
   - 每条待办项展示：完成勾选框、标题、优先级标记、所属分类（大分类 > 子分类）、（可选）提醒时间、操作按钮（编辑 / 删除 / 置顶）。
   - 已完成的待办以删除线 + 置灰展示，默认折叠到列表底部或单独分组。
4. **空状态**：今日没有待办时，展示友好的空状态插画与文案（如「今天清空了，去享受生活吧 🎉」），并引导创建。

**业务规则：**

- 「今日待办」= 当前用户下，`状态 ≠ deleted` **且** `计划日期 = 今天` 的所有待办。
- 进入页面时触发 4.5 的**次日自动延续**逻辑（逾期 pending 的待办计划日期更新为今天）。
- 列表数据来自当前用户，**禁止跨用户**。

**前端实现要点：**

- 路由：`/today`
- 创建、完成、删除操作建议**乐观更新**（先改 UI 再等接口），失败再回滚，保证流畅。
- 完成勾选建议带一个轻微的动画反馈。

#### 6.3.2 待办创建

**功能说明：**

支持两种创建方式：① 列表上方的「快速创建」；② 点击「新建」按钮打开完整表单。

**字段：**

| 字段 | 必填 | 说明 |
|------|------|------|
| 标题 | 是 | 最长 100 字符 |
| 大分类 | 是 | 下拉选择，默认选中「上次使用的分类」或「公司事务」 |
| 子分类 | 否 | 依赖大分类联动，下拉选择；允许为「无」 |
| 计划日期 | 是 | 默认今天；可改为今天 / 明天 / 指定日期 |
| 优先级 | 否 | 默认 medium |
| 备注 | 否 | 最长 2000 字符 |
| 提醒时间 | 否（P1） | 到点推送 / 高亮提醒 |

**业务规则：**

- 计划日期默认今天。
- 快速创建：仅需标题，其余字段取默认值，**回车即提交**。
- 创建成功后，若当前在「今日」视图且计划日期为今天，则新待办出现在列表中并高亮一下。

**前端实现要点：**

- 快速创建输入框始终聚焦友好，支持 `Enter` 提交、`Esc` 清空。
- 大分类切换时，子分类下拉要联动刷新。

#### 6.3.3 待办编辑

- 点击待办的编辑按钮，弹出编辑面板 / 抽屉 / 弹窗。
- 所有字段均可编辑。
- 编辑保存后列表实时更新。
- 校验规则同创建。

#### 6.3.4 待办完成 / 撤销完成

- 点击勾选框：pending → done，记录 `doneAt = 当前时间`。
- 再次点击：done → pending，清空 `doneAt`。
- 已完成的待办在列表中样式变化（删除线、置灰、移到底部）。
- 完成时建议有动效与（可选）轻量正向反馈。

#### 6.3.5 待办删除 / 恢复

- 删除为软删除（进入回收站）。
- 删除前建议二次确认（或支持撤销 toast）。
- 回收站（P1）内可恢复，恢复规则见 4.7。

#### 6.3.6 待办详情

- 点击待办标题或卡片，打开详情抽屉 / 页面。
- 展示完整字段：标题、备注、分类、优先级、计划日期、创建时间、完成时间、状态。
- 详情内可直接编辑、完成、删除。

---

### 6.4 分类管理模块（两层分类）

> 这是用户明确提出的核心需求：**一层大分类 + 二层子分类**。

#### 6.4.1 大分类（Category）

**功能说明：**

用户自定义的顶层分类，如「公司事务」「生活事务」「学习成长」。

**字段：**

| 字段 | 必填 | 说明 |
|------|------|------|
| 名称 | 是 | 最长 20 字符，同用户下唯一 |
| 图标 / 颜色 | 否（P1） | 用于视觉区分，如不同颜色色块 |
| 排序 | 否 | 用户可拖拽调整顺序 |

**业务规则：**

- 新注册用户**自动创建默认大分类**：公司事务、生活事务、学习成长（3 个，可改可删）。
- 大分类可新增、编辑（改名 / 改色）、删除、排序。
- **删除大分类的规则（重要）**：
  - 删除前需提示该分类下有待办数量；
  - 提供「连同子分类与待办一起删除」与「将待办迁移到其他分类」两种方式；
  - 为避免误删，删除大分类必须二次确认，且建议进入回收站（软删）。

#### 6.4.2 子分类（Subcategory）

**功能说明：**

隶属于某个大分类下的细分，如大分类「公司事务」下的「A 项目」「周报」「会议」。

**字段：**

| 字段 | 必填 | 说明 |
|------|------|------|
| 名称 | 是 | 最长 20 字符，同一大分类下唯一 |
| 所属大分类 | 是 | 外键关联 |
| 排序 | 否 | 同大分类下可拖拽排序 |

**业务规则：**

- 子分类必须挂在一个大分类下，不能独立存在。
- 大分类被删除时，其下子分类按 6.4.1 的删除策略处理（一起删或迁移）。
- 待办的子分类允许为空（只选大分类）。

**前端实现要点：**

- 分类管理页：左侧大分类列表，右侧选中大分类的子分类列表（典型的主从布局）。
- 大分类与子分类都支持拖拽排序。

---

### 6.5 视图与筛选模块

#### 6.5.1 按日期查看

**功能说明：**

除「今天」外，支持查看「明天」「指定日期」的待办。

**业务规则：**

- 提供一个日期选择器（迷你日历或左右切换箭头）。
- 选中某天后，展示该天 `计划日期 = 所选日期` 的待办。
- 切换日期时**不触发**自动延续（只有「今天」触发），仅做查询。

#### 6.5.2 按分类查看

**功能说明：**

点击某个大分类 / 子分类，查看该分类下的所有待办。

**业务规则：**

- 支持按大分类查看（包含其下所有子分类的待办）。
- 支持按子分类精确查看。
- 列表内可按状态（全部 / 未完成 / 已完成）二次筛选。
- 默认按计划日期升序，同日按优先级。

#### 6.5.3 搜索

**功能说明：**

通过关键字搜索待办。

**业务规则：**

- 搜索范围：标题 + 备注。
- 只在当前用户数据范围内搜索。
- 支持按状态、分类二次过滤（P1）。
- 关键字高亮展示（P1）。

---

### 6.6 效率增强模块

> 这些是产品负责人基于经验新增的「提效」功能，非用户原始需求，但对个人效率提升明显。

#### 6.6.1 置顶（Pin）

- 任意待办可置顶，置顶后始终排在列表最前（在排序规则 4.6 中权重最高）。
- 再次点击取消置顶。
- 字段：`isPinned: boolean`。

#### 6.6.2 提醒（Reminder）（P1）

**功能说明：**

为待办设置一个提醒时间，到点提醒用户。

**业务规则：**

- 提醒时间必须 ≥ 当前时间。
- 提醒方式：浏览器通知（Web Notification）、站内消息高亮；（移动端可后续扩展推送）。
- 提醒时间到达时：
  - 若待办已完成，则不提醒；
  - 否则触发提醒，并标记 `已提醒`。
- 用户可在设置中开关「提醒通知」总开关。

#### 6.6.3 重复待办（Repeat）（P2）

**功能说明：**

支持设置周期性待办，如「每周五写周报」。

**业务规则：**

- 支持频率：每天 / 每周（指定星期几）/ 每月（指定几号）/ 工作日。
- 实现方式：每次到达一个周期，自动生成一条新的待办实例（计划日期为该周期日）。
- 删除重复待办时：可选「仅删除本次」「删除本次及以后所有」。

#### 6.6.4 子任务 / 清单（Subtasks）（P2）

**功能说明：**

一条待办下可拆分多个子任务，支持独立勾选。

**业务规则：**

- 子任务有标题与完成状态。
- 父待办展示子任务完成进度（如 3/5）。
- 全部子任务完成时，**不自动**完成父待办（避免误判），但可提示「子任务已全部完成」。

#### 6.6.5 标签（Tags）（P2）

- 跨分类的横向标记，如 `紧急`、`可委派`、`碎片时间`。
- 一个待办可挂多个标签。
- 支持按标签筛选。

---

### 6.7 回顾统计模块

> 帮助用户回顾自己的时间投入，持续优化。MVP 可只做「今日概览」。

#### 6.7.1 今日概览（P0/P1）

- 今日待办总数、已完成数、完成率、进度条。
- 按大分类展示今日分布（如「公司事务 5 项 / 生活 2 项」）。

#### 6.7.2 完成趋势（P2）

- 近 7 天 / 30 天每日完成数量折线图。
- 帮助识别效率波动。

#### 6.7.3 分类占比（P2）

- 近 30 天各分类完成数占比（饼图）。
- 帮助识别时间花在哪里。

---

### 6.8 设置模块

#### 6.8.1 个人资料

- 查看 / 修改昵称、头像（P2）。
- 修改邮箱（需校验唯一）。

#### 6.8.2 修改密码

- 需输入旧密码 + 新密码 + 确认新密码。
- 新密码规则同注册。

#### 6.8.3 偏好设置

- 默认优先级、默认大分类。
- 「每日开始时间」（影响「今天」的计算，默认 00:00，部分用户希望按 04:00 翻日）。
- 提醒通知总开关（P1）。
- 主题：浅色 / 深色 / 跟随系统（P2）。

---

## 7. 全局业务流程

### 7.1 新用户首次使用流程

```
访问首页 → 点「立即开始」→ 注册 → 自动登录
   → 自动生成默认分类（公司/生活/学习）
   → 进入今日待办（空状态，引导创建第一条）
   → 创建第一条待办 → 出现在今日列表
```

### 7.2 日常使用主流程

```
打开系统（已登录）
   → 若是首次进入「今天」，先执行次日自动延续（逾期 pending → 计划日期改今天）
   → 展示今日待办 + 今日概览
   → 用户逐条勾选完成 / 编辑 / 删除
   → 剩余未完成的，次日自动延续
```

### 7.3 待办生命周期

```
创建（pending，计划日期=今天）
   → 当天完成 → done（记录 doneAt）
   → 当天未完成 → 次日自动延续（计划日期=明天，即新的今天）
   → 任意时刻删除 → 进入回收站 → 可恢复或 30 天后清除
```

---

## 8. 非功能需求

### 8.1 性能

- 今日待办列表首屏加载 ≤ 1 秒（100 条以内）。
- 创建 / 完成 / 删除等操作响应 ≤ 300ms（前端乐观更新，体感即时）。
- 列表分页或虚拟滚动，单页加载上限 100 条。

### 8.2 安全

- 密码加密存储（bcrypt 等单向哈希），禁明文。
- 所有接口强制鉴权，待办 / 分类接口强制按 `当前用户 ID` 过滤。
- 防 SQL 注入（参数化查询）、防 XSS（输出转义）。
- 登录失败次数限制（见 6.2.2）。
- 敏感操作（删除分类等）二次确认。

### 8.3 兼容性

- 浏览器：Chrome、Edge、Safari、Firefox 近 2 个大版本。
- 响应式：桌面端为主，需兼容移动端浏览器（竖屏可用）。
- 移动端原生 App 可作为后续迭代。

### 8.4 可用性 / 健壮性

- 软删除 + 回收站，防误删。
- 接口失败时前端有明确错误提示，且支持重试。
- 网络断开时，前端尽量缓存待办列表，恢复后同步。

---

## 9. 数据模型概览（供后端参考）

> 仅给出字段与关系，具体类型由后端按技术栈决定。所有表均带 `createdAt`、`updatedAt`。

### 9.1 用户表 `users`

| 字段 | 说明 |
|------|------|
| id | 主键 |
| username | 用户名，唯一 |
| email | 邮箱，唯一 |
| passwordHash | 密码哈希 |
| nickname | 昵称 |
| timezone | 时区（如 Asia/Shanghai） |
| defaultCategoryId | 默认大分类 |
| reminderEnabled | 提醒总开关 |
| createdAt / updatedAt | 时间戳 |

### 9.2 大分类表 `categories`

| 字段 | 说明 |
|------|------|
| id | 主键 |
| userId | 所属用户（数据隔离） |
| name | 名称（同用户唯一） |
| color | 颜色（P1） |
| icon | 图标（P1） |
| sortOrder | 排序 |
| deletedAt | 软删除时间，null 表示未删 |

### 9.3 子分类表 `subcategories`

| 字段 | 说明 |
|------|------|
| id | 主键 |
| categoryId | 所属大分类 |
| userId | 所属用户（冗余，便于隔离查询） |
| name | 名称（同大分类下唯一） |
| sortOrder | 排序 |
| deletedAt | 软删除 |

### 9.4 待办表 `todos`

| 字段 | 说明 |
|------|------|
| id | 主键 |
| userId | 所属用户（数据隔离） |
| title | 标题 |
| note | 备注 |
| categoryId | 大分类 |
| subcategoryId | 子分类，可空 |
| priority | high / medium / low |
| status | pending / done / deleted |
| planDate | 计划日期（精确到天） |
| doneAt | 完成时间，可空 |
| isPinned | 是否置顶 |
| reminderAt | 提醒时间，可空 |
| reminderSent | 是否已提醒 |
| deletedAt | 软删除时间 |
| createdAt / updatedAt | 时间戳 |

**关键索引建议：**
- `(userId, planDate, status)` —— 今日待办查询主索引。
- `(userId, categoryId, status)` —— 分类视图查询。
- `(userId, status, deletedAt)` —— 回收站查询。

---

## 10. 页面清单与路由（供前端参考）

| 路由 | 页面 | 是否需登录 | 说明 |
|------|------|-----------|------|
| `/` | 首页 / 落地页 | 否 | 已登录则重定向 `/today` |
| `/login` | 登录页 | 否 | 已登录则重定向 `/today` |
| `/register` | 注册页 | 否 | 已登录则重定向 `/today` |
| `/today` | 今日待办 | 是 | 主工作台，默认页 |
| `/date/:date` | 指定日期待办 | 是 | 如 `/date/2026-06-29` |
| `/category/:id` | 分类详情 | 是 | 某大分类 / 子分类下的待办 |
| `/search` | 搜索结果 | 是 | 带 `?q=关键字` |
| `/categories` | 分类管理 | 是 | 两层分类增删改排序 |
| `/stats` | 回顾统计 | 是 | 概览 + 趋势（P2） |
| `/trash` | 回收站 | 是 | P1 |
| `/settings` | 设置 | 是 | 资料 / 密码 / 偏好 |
| `/todo/:id` | 待办详情 | 是 | 可选，也可用抽屉实现 |

**前端布局建议：**
- 登录后页面采用「左侧导航 + 右侧内容」布局。
- 左侧导航：今日、日期、分类列表（树形）、搜索、回收站、统计、设置。
- 顶部：当前用户信息、退出登录。

---

## 11. 接口清单概览（供后端参考）

> 仅列出资源与方法，具体入参出参由后端按 RESTful 风格细化。所有接口除「注册 / 登录」外均需鉴权，且强制 `userId` 过滤。

### 11.1 认证

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/register` | 注册 |
| POST | `/api/auth/login` | 登录 |
| POST | `/api/auth/logout` | 退出 |
| GET | `/api/auth/me` | 获取当前用户信息 |

### 11.2 待办

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/todos?date=today` | 今日待办（触发自动延续） |
| GET | `/api/todos?date=YYYY-MM-DD` | 指定日期 |
| GET | `/api/todos?categoryId=&subcategoryId=` | 按分类 |
| GET | `/api/todos/search?q=` | 搜索 |
| POST | `/api/todos` | 创建 |
| GET | `/api/todos/:id` | 详情 |
| PUT | `/api/todos/:id` | 编辑 |
| PATCH | `/api/todos/:id/status` | 完成 / 撤销（body: `{status}`） |
| PATCH | `/api/todos/:id/pin` | 置顶 / 取消（body: `{isPinned}`） |
| DELETE | `/api/todos/:id` | 软删除（进回收站） |
| GET | `/api/todos/trash` | 回收站列表（P1） |
| POST | `/api/todos/:id/restore` | 恢复（P1） |
| DELETE | `/api/todos/:id/purge` | 彻底删除（P1） |

### 11.3 分类

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/categories` | 大分类列表（含子分类树） |
| POST | `/api/categories` | 新增大分类 |
| PUT | `/api/categories/:id` | 编辑 |
| DELETE | `/api/categories/:id` | 删除（body 指定迁移或连带删除策略） |
| PATCH | `/api/categories/reorder` | 排序 |
| POST | `/api/categories/:id/subcategories` | 新增子分类 |
| PUT | `/api/subcategories/:id` | 编辑子分类 |
| DELETE | `/api/subcategories/:id` | 删除子分类 |

### 11.4 统计

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/stats/today` | 今日概览 |
| GET | `/api/stats/trend?range=7d` | 完成趋势（P2） |
| GET | `/api/stats/category-share?range=30d` | 分类占比（P2） |

### 11.5 设置

| 方法 | 路径 | 说明 |
|------|------|------|
| PUT | `/api/users/profile` | 改资料 |
| PUT | `/api/users/password` | 改密码 |
| PUT | `/api/users/preferences` | 改偏好 |

---

## 12. 版本规划（分期建议）

### V1.0（MVP，对应 P0）

目标：跑通核心闭环，能真正用起来。

- 首页介绍
- 注册 / 登录 / 退出
- 两层分类（含默认分类自动生成）
- 今日待办：创建、编辑、完成 / 撤销、删除
- 次日自动延续
- 今日概览（简单统计条）

### V1.1（对应 P1）

目标：好用、不丢、可查。

- 日期视图、分类视图、搜索
- 置顶
- 回收站与恢复
- 提醒（浏览器通知）
- 分类颜色 / 图标
- 完成趋势图

### V2.0（对应 P2）

目标：更智能、更强回顾。

- 重复待办
- 子任务
- 标签
- 分类占比图
- 导出（CSV / JSON）
- 深色模式
- 移动端适配优化 / PWA

---

## 13. 验收标准与审核清单（产品负责人自用）

> 本节供产品负责人在前后端 AI 输出后逐项审核，确保实现符合需求。

### 13.1 前端审核清单

- [ ] 未登录访问 `/` 显示介绍页，已登录访问 `/` 自动跳转 `/today`。
- [ ] 未登录访问需鉴权页面，被拦截并跳转登录页（带 redirect 回跳）。
- [ ] 注册 / 登录表单校验完整，错误提示清晰，防重复提交。
- [ ] 今日待办页：展示统计条 + 进度条 + 列表 + 空状态。
- [ ] 快速创建：仅标题即可回车提交。
- [ ] 完成勾选有样式变化（删除线 / 置灰 / 移到底部）与动效。
- [ ] 列表排序符合 4.6（优先级 > 状态 > 创建时间 > 置顶）。
- [ ] 两层分类：主从布局，大分类下展示子分类，支持增删改排序。
- [ ] 删除大分类有二次确认与策略选择。
- [ ] 日期 / 分类 / 搜索视图可用。
- [ ] 操作采用乐观更新，失败有错误提示与回滚。
- [ ] 响应式：桌面与移动竖屏均可用。

### 13.2 后端审核清单

- [ ] 密码哈希存储，非明文。
- [ ] 所有待办 / 分类接口强制 `userId` 过滤，无越权。
- [ ] 今日待办查询触发「次日自动延续」逻辑。
- [ ] 删除均为软删除，回收站可恢复，30 天清理。
- [ ] 状态机流转正确（pending / done / deleted，撤销与恢复）。
- [ ] 计划日期按用户本地时区的自然日判断。
- [ ] 关键索引（userId+planDate+status 等）已建。
- [ ] 接口符合 RESTful，错误码与提示规范。

### 13.3 数据安全审核清单

- [ ] 无 SQL 注入、XSS 漏洞。
- [ ] 登录失败次数限制生效。
- [ ] 任何接口都不能仅凭资源 ID 访问到他人数据。

---

## 附录 A：术语表

| 术语 | 说明 |
|------|------|
| PRD | Product Requirements Document，产品需求文档 |
| MVP | Minimum Viable Product，最小可行产品 |
| P0/P1/P2 | 功能优先级分级 |
| 软删除 | 标记删除而非物理删除，可恢复 |
| 乐观更新 | 前端先更新 UI 再等接口返回，提升体感速度 |
| RESTful | 一种 API 设计风格 |

---

**文档结束。**

> 后续前端 AI 与后端 AI 可分别依据「第 6 章 功能详细需求」「第 9 章 数据模型」「第 10 章 路由」「第 11 章 接口」进行实现。实现完成后，产品负责人将依据「第 13 章 验收清单」进行审核。
